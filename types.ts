export interface ArchiveItem {
    id: string;
    date: string;
    tag: string;
    tagIcon: string;
    question: string;
    count: number;
}

export interface QuestionData {
    id: string;
    text: string;
    date: string;
    ticketNum: string;
}
